---
import { sourceUrl } from '../../config';

interface Props {
  path: string;
  label?: string;
  view?: 'blob' | 'tree';
}

const { path, label = path, view = 'blob' } = Astro.props;
const href = sourceUrl(path, view);
---

<a href={href}><slot>{label}</slot></a>
