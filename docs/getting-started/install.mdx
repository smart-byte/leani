---
title: Install Leani
description: Build the current alpha, check the binary, and prepare the TypeScript SDK if you need it.
section: getting-started
order: 20
audience:
  - operator
  - app-developer
status: preview
---

import { Aside } from '@astrojs/starlight/components';
import CodeExample from '@docs/components/docs/CodeExample.astro';

## Build the current alpha

Use a source checkout while the first release packages are being prepared.
You need Git, Rust installed through rustup, and your platform's C/C++ build
tools. The repository's `rust-toolchain.toml` selects Rust 1.97.0 automatically.
The first build downloads dependencies and can take several minutes.

<CodeExample example="source-install" />

Expect `leani 0.1.0`. If you already have the checkout, start at
`cargo build --locked -p leani` in its root directory.

`LEANI_SOURCE` points to the checkout; later tutorials use it to find example
configs. The `PATH` setting makes `leani` available in this terminal. In each
additional terminal, return to the checkout and run:

```bash
export LEANI_SOURCE="$PWD"
export PATH="$LEANI_SOURCE/target/debug:$PATH"
```

Continue with [your first Ethereum block](/docs/getting-started/).
For a check without network access, run the
[offline fixture](/docs/getting-started/offline-proof/).

## Prepare the TypeScript SDK

The examples use Bun 1.2 or later. From the checkout:

```bash
bun install --cwd packages/sdk --frozen-lockfile
bun run --cwd packages/sdk build
```

Repository examples resolve `@leani/sdk` to the local source. To use the built
package in your own application, run this from that application's directory:

```bash
bun add "$LEANI_SOURCE/packages/sdk"
```

Then follow the [query and subscription example](/docs/guides/query-then-follow/).
For Rust applications, see [build a custom node](/docs/guides/custom-processor/).

## Published releases

<Aside type="note" title="Available after publication">
  The commands in this section require the corresponding public release
  artifacts. Use the source instructions above for the current alpha.
</Aside>

Once published, install with `brew install smart-byte/tap/leani`, or choose a
matching archive from the [GitHub releases](https://github.com/smart-byte/leani/releases).
Verify the archive's published checksum before running it. Containers use
`ghcr.io/smart-byte/leani:<version>`; pin the release's digest for deployment.
The SDK package is `@leani/sdk` on npm. Keep binary, SDK, and configuration
versions aligned with the release you selected.
