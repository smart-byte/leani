---
title: Follow your first Ethereum block
description: Install the alpha, read a real Ethereum block, and prepare a node for your application.
section: getting-started
order: 10
audience:
  - app-developer
  - operator
status: preview
---

import { Aside } from '@astrojs/starlight/components';
import CodeExample from '@docs/components/docs/CodeExample.astro';

[Install the current alpha](/docs/getting-started/install/) first. This example
prints a real Ethereum Mainnet block with no API key and no configuration file.
Run it from a fresh directory:

```bash
cd "$(mktemp -d)"
```

<CodeExample example="live-block-first-run" />

On first use, Leani asks independent public providers for a recent finalized
checkpoint. At least two must agree before you can accept it. This is the
starting trust anchor for subsequent verification.

Peer discovery can take several minutes, even on a later run with cached state.
Leani prints progress to stderr. Success is a line shaped like:

```text
2026-09-07T12:11:59Z  block=25925396  txs=381  gas=43.17M / 60.00M (72.0%)  base_fee=0.069459109 gwei  blobs=3  included
```

Your block number and values will differ. `--once` exits after that first result.
Remove it to keep following blocks, or add `--format json` for scripts.

<Aside type="note" title="Read the finality label">
  The first result may say `preview`: a peer supplied the block and Leani is
  still checking its place in Ethereum's chain. JSON reports
  `finality: "preview"`. Later rows say `included` (on the followed chain, a
  reorg can still remove them) or `finalized`. Use
  `leani subscribe blocks --finality finalized --once` when you need a result
  Ethereum consensus has finalized.
</Aside>

## Prepare a node for your application

When you want an HTTP API and SDK subscriptions, initialize a reusable node:

<CodeExample example="live-block-node" />

Leave `serve` running. In another terminal, set up `leani` as described on the
install page and change into the same `leani-blocks` directory. Then run:

```bash
leani subscribe blocks --format json
```

It attaches to the running node. The API is at `http://127.0.0.1:18080` and
JSON-RPC is at `http://127.0.0.1:18545`. Discover the configured processor with:

```bash
curl -s http://127.0.0.1:18080/v1/capabilities
```

Expect a `block-summary` processor with `subscriptions: true`. Keep each node's
data directory separate; do not start two nodes against the same writable data.

## Choose your next example

- [Index a small historical range](/docs/getting-started/bounded-mainnet/) to query decoded contract events.
- [Query and follow with TypeScript](/docs/guides/query-then-follow/) to seed application state.
- [Build a custom processor in Rust](/docs/guides/custom-processor/) to run your own transformation using the Leani library.

For startup diagnostics and all CLI fields, see
[block subscriptions](/docs/guides/live-block-subscriptions/). Read
[alpha limitations](/docs/operations/preview-limitations/) before connecting production applications.
